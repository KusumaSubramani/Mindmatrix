package com.kashta.kala.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette — Warm Amber (Woodcraft warmth) ─────────────────────
val PrimaryAmber = Color(0xFFF57C00)          // Deep amber orange
val PrimaryAmberDark = Color(0xFFE65100)
val PrimaryAmberLight = Color(0xFFFFB74D)

// ── Secondary Palette — Rich Teak Brown ─────────────────────────────────
val SecondaryBrown = Color(0xFF5D4037)         // Dark teak brown
val SecondaryBrownLight = Color(0xFF8D6E63)
val SecondaryBrownDark = Color(0xFF3E2723)

// ── Tertiary — Forest Green accent ──────────────────────────────────────
val TertiaryGreen = Color(0xFF2E7D32)
val TertiaryGreenLight = Color(0xFF66BB6A)

// ── Surfaces — Light Theme ──────────────────────────────────────────────
val BackgroundLight = Color(0xFFFFF8F0)        // Warm cream
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFFFF3E0)    // Light amber tint
val OnSurfaceLight = Color(0xFF1B1B1F)
val OnSurfaceVariantLight = Color(0xFF4E4539)

// ── Surfaces — Dark Theme ───────────────────────────────────────────────
val BackgroundDark = Color(0xFF1A1410)         // Dark walnut
val SurfaceDark = Color(0xFF231C15)
val SurfaceVariantDark = Color(0xFF2D2418)
val OnSurfaceDark = Color(0xFFF5E6D3)
val OnSurfaceVariantDark = Color(0xFFD7CCC8)

// ── Status Colors ───────────────────────────────────────────────────────
val SuccessGreen = Color(0xFF4CAF50)
val ErrorRed = Color(0xFFEF5350)
val WarningAmber = Color(0xFFFFB300)

// ── Furniture Category Accent Colors ────────────────────────────────────
val SofaColor = Color(0xFF7B1FA2)              // Purple
val BedColor = Color(0xFF1565C0)               // Blue
val CabinetColor = Color(0xFFE65100)           // Deep Orange
val TableColor = Color(0xFF00838F)             // Teal
val ChairColor = Color(0xFF558B2F)             // Light Green

// ── Gradient Colors ─────────────────────────────────────────────────────
val GradientStart = Color(0xFFF57C00)          // Amber
val GradientMiddle = Color(0xFF8D6E63)         // Brown
val GradientEnd = Color(0xFF5D4037)            // Dark Teak

// ── Wood Type Colors ────────────────────────────────────────────────────
val TeakWood = Color(0xFF8B6914)
val SheeshamWood = Color(0xFF6D4C41)
val PineWood = Color(0xFFD4A574)
val MangoWood = Color(0xFFA1887F)
val SalWood = Color(0xFF795548)
